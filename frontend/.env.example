# Public backend URL only. Never put API keys or other secrets in VITE_* variables.
VITE_API_URL=http://localhost:8000
# Optional public KIET Admissions WhatsApp number in international digits (91 + 10 digits).
# Leave blank until KIET provides its official admissions contact number.
VITE_ADMISSIONS_WHATSAPP_NUMBER=
